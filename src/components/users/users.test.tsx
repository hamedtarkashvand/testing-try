import { render , Screen } from "@testing-library/react";
import { Users } from "./users";

describe('d', () => {
    const data = ['css' , 'js' , 'html']
    test('ds', () => {
        render(<Users  />)
    })
})